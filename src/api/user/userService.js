const User = require('../user/User')
const { sendErrorsFromDB } = require('../../shared/utils')

const deletarUsuario = (req, res, next) => {
  // Precisa trabalhar com função de usuário aqui
  // Só que pode apagar é o próprio usuário ou um admin
}